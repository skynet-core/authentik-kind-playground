kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: traefik
  namespace: default
spec:
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "1G"
  storageClassName: "standard"
